'use strict';

const isObject = (value) => (typeof(value) === 'object');

export default (state) => ({
  teach: function(categories) {
    if (isObject()) {
      debugger;
      state.intents.push({
        script: script,
        actions: actions
      });
    }

    return this;
  }
});
